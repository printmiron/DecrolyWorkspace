<!DOCTYPE html>
<html>
<body>

<?php

//ejercicio1
$numeros = [1,2,3,4,5];

//suma total
foreach ($numeros as $numero){
	$result_sum += $numero;
    
}
//promedio
function media ($arr){
	$suma = array_sum($arr);
    $cantidad = count($arr);
	
    if ($cantidad > 0){
    	return $suma / $cantidad;
    } else {
    	return 0;
    }
}

$promedio = media($numeros);

//minimo
echo "El numero maximo es: " . min($numeros) . "<br>";
//maximo
echo "El numero minimo es: " . max($numeros) . "<br>";

echo "Media del array es: " . $promedio . "<br>";
echo "Suma de todos los numeros: " . $result_sum . "<br>";
	
//ejercicio2
$dado1 = rand(1, 6);
$dado2 = rand(1, 6);

echo "Dado 1: " . $dado1 . "<br>";
echo "Dado 2: " . $dado2 . "<br>";

$suma_dados = $dado1 + $dado2;

echo "Suma de los dados es: " . $suma_dados . "<br>";

if ($dado1 === $dado2){
	echo "Dados son dobles" . "<br>";
}else{
	echo "Dados no son dobles" . "<br>";
}

//ejercicio3
$notas = [
  "Ana" => 8.5,
  "Lius" => 6.2,
  "Marta" => 9.1,
  "Pedro" => 4.8
];


//promedio
function mediaNotas ($notas){
	$suma = array_sum($notas);
    $cantidad = count($notas);
	
    if ($cantidad > 0){
    	return $suma / $cantidad;
    } else {
    	return 0;
    }
}

$promedioNotas = mediaNotas($notas);

echo "Media de las notas es: " . $promedioNotas . "<br>";

echo "El alumno con mejor nota es: " . max($notas) . "<br>";

$aprobados = 0;
foreach ($notas as $n){
	if($notas >= 5) $aprobados++;
}

echo "Alumnos aprobados con mas de 5: " . $aprobados . "<br>";


//ejercicio4 ()
$numerosS = [11,7,20,50,33,78,2,10,4,99,43,27,16,17,64,29,30,92,51,66];
//pares
$pares = 0;
foreach ($numerosS as $np) {
    if ($np % 2 == 0) $pares++;
}
echo "Hay $pares numeros pares en el array" . "<br>";


//impares
$impares = 0;
foreach ($numerosS as $ni) {
    if ($ni % 2 == 1) $impares++;
}
echo "Hay $impares numeros impares en el array" . "<br>";


//ejercicio5
$alumnos = [
    "Ana" => ["PHP" => 8.5, "HTML" => 9.0, "CSS" => 7.5],
    "Luis" => ["PHP" => 4.8, "HTML" => 6.2, "CSS" => 5.9],
    "Marta" => ["PHP" => 9.1, "HTML" => 8.8, "CSS" => 9.5],
    "Pedro" => ["PHP" => 5.2, "HTML" => 4.1, "CSS" => 5.0],
];


//accede a la posicion 0 "Ana" pero no entiendo como acceder a la primero y otras
function mediaAna ($alumnos){
      foreach ($alumnos as $a){
      $suma = array_sum($a);
      $cantidad = count($a);

      if ($cantidad > 0){
          return $suma / $cantidad;
      } else {
          return 0;
      }
  }
}

$promedioAna = mediaAna($alumnos);

echo "Media de Ana es: " . $promedioAna . "<br>";





?>

</body>
</html>

